name: Docker Image CI

on:
  push:
    branches: [ "product" ]
  pull_request:
    branches: [ "product" ]



jobs:

  build:

    runs-on: self-hosted
    env: 
      TAG: $(date +%s)
    steps:
    - uses: actions/checkout@v4
 #   - name: Build the Docker image
  #    run: make  IMAGE=caydic:$TAG build
   # - name : Erase container
    #  run : make erase 
   # - name : Create container
    #  run : make IMAGE=caydic:$TAG run
    - name : Affiche TAG
      run : echo $TAG
   
